# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files
.env
.env*.local

# typescript
*.tsbuildinfo

# local data (SQLite + uploads)
/data/

# lockfiles from other package managers must never appear (Bun only)
package-lock.json
yarn.lock
pnpm-lock.yaml

# raw key drop file (migrated to .env.local)
env.txt
.playwright-cli/
